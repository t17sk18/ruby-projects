<%= form_with(model: product, local: true) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
        <% product.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :productId %>
    <%= form.number_field :productId %>
  </div>

  <div class="field">
    <%= form.label :productName %>
    <%= form.text_field :productName %>
  </div>

  <div class="field">
    <%= form.label :productDescription %>
    <%= form.text_field :productDescription %>
  </div>

  <div class="field">
    <%= form.label :productPrice %>
    <%= form.text_field :productPrice %>
  </div>

  <div class="field">
    <%= form.label :productColor %>
    <%= form.text_field :productColor %>
  </div>

  <div class="field">
    <%= form.label :productSize %>
    <%= form.text_field :productSize %>
  </div>

  <div class="field">
    <%= form.label :productAvailability %>
    <%= form.number_field :productAvailability %>
  </div>

  <div class="field">
    <%= form.label :productImage %>
    <%= form.text_area :productImage %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
